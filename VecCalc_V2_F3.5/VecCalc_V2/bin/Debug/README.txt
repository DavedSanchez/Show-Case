Vector Calculator:
	Des:
		First attempt at making a GUI-based desktop appliction
		First attempt to practically use C#
		Uses Model-View-ViewModel Framework
		Takes two vectors (A and B) then adds the two into a resulting vec
			output is A, B, and R vec with math stats